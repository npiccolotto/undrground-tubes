{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.com/product.schema.json",
  "title": "ESVis Input",
  "description": "A valid ESVis input",
  "type": "object",
  "required": [
    "S",
    "E",
    "EA",
    "SR"
  ],
  "properties": {
    "glyph_ids": {
      "description": "paths to glyph images. i-th entry holds image for i-th element.",
      "type": "array"
    },
    "E": {
      "description": "labels for elements",
      "type": "array"
    },
    "EA": {
      "description": "distance matrix (list of list) for element data",
      "type": "array"
    },
    "SA": {
      "description": "distance matrix (list of list) for set data",
      "type": "array"
    },
    "SR": {
      "description": "set relations, as list of list. i-th entry holds sets that element i belongs to. ",
      "type": "array"
    },
    "S": {
      "description": "labels for sets",
      "type": "array"
    },
    "SC": {
      "description": "colors for sets in the drawing as hex string",
      "type": "array"
    }
  }
}
